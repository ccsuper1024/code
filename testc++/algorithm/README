这是C++ Primer 附录A A2算法概览的示例
标准库定义了超过100个算法。要高效使用这些算法，需要了解他们的结构而不是单纯记忆每个算法的细节。
简要描述：
    1. beg和end是表示元素范围的迭代器。几乎所有的算法都对一个由beg和end表示大序列进行操作
    2. beg2是表示第二个输入序列开始位置的迭代器。end2表示的是第二个序列的末尾位置(如果有的话)。如果没有end2，则嘉定beg2表示的序列
       与beg和end的序列一样大。beg和beg2的类型不必匹配，但是，必须保证对两个序列中的元素都可以执行特定操作或调用给定的可调用对象
    3. dest是表示目的序列的迭代器。对于给定输入序列，算法需要生成多少元素，目的序列必须保证能保存同样多的元素。
    4. unaryPred和binaryPred是一元和二元谓词(接受1个或2个参数的可调用对象，有返回值)，分别接受一个或两个参数，都是来自输入序列的元素，
       两个谓词都返回可用作条件的类型。
    5. comp是一个二元谓词，满足关联容器中对关键字序的要求
    6. unaryOp和binaryOp是可调用对象，可分别使用来自输入序列的一个和两个实参来调用。
